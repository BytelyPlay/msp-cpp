module;
#include <string>
#include <vector>

export module Base64Utils;

export namespace Base64Utils
{
    /*
    std::string encode(std::vector<unsigned char> bytes, bool& success);
    std::vector<unsigned char> decode(std::string base64, bool& success);
    */
}
